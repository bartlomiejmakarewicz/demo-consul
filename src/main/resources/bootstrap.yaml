spring:
  application:
    name: demo-consul
  cloud:
    consul:
      host: consul
      config:
        fail-fast: false
    config:
      discovery:
        enabled: true
        service-id: cloud-config-service
