spring:
  application:
    name: demo-consul
  cloud:
    config:
      uri: http://cloud-config-service:8888
      fail-fast: true
      discovery:
        enabled: true
        service-id: cloud-config-service
    consul:
      host: consul
